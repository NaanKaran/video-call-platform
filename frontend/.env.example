# Example environment variables for the frontend
# Copy this file to .env and fill in your values

# Backend API configuration
VITE_API_BASE_URL=https://your-backend-url.com/api
VITE_SOCKET_BASE_URL=https://your-backend-url.com

# LiveKit configuration  
VITE_LIVEKIT_URL=wss://your-livekit-server.com

# Note: In Vite, environment variables must be prefixed with VITE_ to be accessible in the browser
# These values will be embedded in the client-side bundle, so don't put sensitive secrets here